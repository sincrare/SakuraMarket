.under_space
  = simple_form_for [:admin, @item] do |f|
    = f.input :name
    = f.input :price
    = f.input :description
    = f.input :image
    = f.input :active
    = f.button :submit, class: 'btn btn-primary'
